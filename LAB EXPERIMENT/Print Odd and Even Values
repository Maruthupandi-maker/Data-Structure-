#include <stdio.h>

int main() {
    int a[50], n, i;

    printf("Enter size: ");
    scanf("%d", &n);

    printf("Enter array: ");
    for (i = 0; i < n; i++)
        scanf("%d", &a[i]);

    printf("Even values: ");
    for (i = 0; i < n; i++)
        if (a[i] % 2 == 0)
            printf("%d ", a[i]);

    printf("\nOdd values: ");
    for (i = 0; i < n; i++)
        if (a[i] % 2 != 0)
            printf("%d ", a[i]);

    return 0;
}
